---
applications:
- name: config-client-pcf-app
  random-route: true
  instances: 1
  memory: 1G
  env:
    spring.application.name: petDetails
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 12.+ } }'
  services:
    - my-config-server
